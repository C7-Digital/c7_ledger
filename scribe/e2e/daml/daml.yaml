# NOTE: sdk-version must match ledger's @daml/types dependency (currently 3.4.11).
# When bumping, also update .github/workflows/ci.yml (Daml SDK install step).
sdk-version: 3.4.11
name: scribe-e2e-test
version: 0.1.0
source: daml
dependencies:
  - daml-prim
  - daml-stdlib
build-options:
  - -Wno-upgrade-interfaces
